spring:
  application:
    #项目名称
    name: one

  #MySQL数据库连接信息
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dlyk
    username: root
    password: mysql

server:
  #端口号
  port: 8090
